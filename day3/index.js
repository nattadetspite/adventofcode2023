const readData = require("../utils");

const dataArray = readData("exampleInput.txt");

/**
 * find number
 * find lenght number
 * check adjacent around number is there any symbpl
 * if any symbol add it in sum var.
 */

function isNumber (c) {
    if (c >= '0' && c <= '9') return true;
}

for (let i = 0; i < dataArray.length; i++) {
  let startNum = true;
  let stopNum = false;

for (let j = 0; j < dataArray[i].length; j++) {
    
  }
}
